# 工作总结（截至 2026-02-08）

## 项目概述
- 项目：哨笛/竖笛指法生成器（静态网页）
- 功能：输入简谱或字母音名，生成六孔指法；支持打印；指法可自定义并本地保存
- 仓库：git@github.com:waterXP/flute-sheet.git

## 核心功能
- 输入支持连续字符，不依赖空格；仅空白会分段（未知字符不再触发分段）
- 输出按“段”生成：连续输入在同一个白框内展示，分段后新白框
- 单音指法以竖排 6 孔显示（闭孔黑、开孔白、半孔竖向半黑）
- 指法表支持多指法（同一音多种按法）
  - 指法表中横向展示多指法
  - 每个指法可移除，音名右侧可新增
  - 结果区若有多指法，提供切换按钮（仅切换被点击的那一个）
  - 打印时隐藏这些操作按钮
- 指法匹配不再卡八度：若当前八度无指法，会回退到同名音的指法
- 音名显示：
  - 默认字母模式（如 D、F#）
  - 结果区可切换为数字模式（1-7，按映射表显示）
  - 八度差用 + / - 表示（+ 表示高八度，- 表示低八度）

## 主要界面与交互
- 结果区白框宽度随内容自适应（非固定列宽）
- 打印页：只显示结果区；保留孔位填黑；隐藏控制按钮
- 半孔图标为竖向半黑（包括打印页右上角图例）
  - 打印时隐藏图例，只保留结果内容

## 重要实现细节
- 使用本地存储保存指法表，`file://` 场景下加了容错避免阻塞按钮
- 多指法切换按“实例”记录（同名不同位置互不影响）
- 指法解析与数字显示统一使用“简谱标签 → 音名”的单一映射表
- 高/低八度标记改为 `+` / `-`，不再使用 `'` / `.`
- 指法结果标题可编辑并本地保存；打印时用竖排标题栏代替横向标题
- 打印布局：
  - 使用 `card-wrap`+`padding-top` 控制纵向间距，配合 `column-gap` 只保留横向间距
  - 增加打印缩放滑块（仅影响打印）

## 文件清单
- `index.html` 页面结构与控件
- `styles.css` 视觉与打印样式
- `app.js` 解析、渲染、指法逻辑
- `README.md` 使用说明

## 下一步可选
- SVG 图标替换 + / − / ⇄，保证像素级居中
- 为指法表增加导入/导出（JSON）
- 添加部署说明（GitHub Pages）
